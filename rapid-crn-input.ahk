!esc::exitapp

;-----------------------------------------------------------------------

!c::

crn := []

for index, value in crn
	send, %value%{tab}

return

;-----------------------------------------------------------------------

!x::

loop, X
{
	send, ^a
	send, {backspace}
	send, {tab}
}
return

;-----------------------------------------------------------------------